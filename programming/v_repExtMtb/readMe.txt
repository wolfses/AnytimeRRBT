This is a Qt project. You can compile it, and use the generated plugin only if:

- you compile it with the Qt framework (e.g. Qt Creator)
- you use the exact same Qt version as V-REP is using (see the about box in the application)
- you use the same compiler as V-REP was compiled with:
    Windows: MinGW
	Mac: GCC
	Linux: GCC